<%= simple_form_for [:admin, @school_medium], :html => {:class => "form-horizontal"} do |f| %>
    <% if @school_medium.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@school_medium.errors.count, "error") %> prohibited this role from being saved:</h2>

          <ul>
            <% @school_medium.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <fieldset>

      <!-- Text input-->
      <div class="control-group">
        <label class="control-label" for="textinput">Name</label>

        <div class="controls">
          <%= f.input :name, label: false %>

        </div>
      </div>

      <!-- Textarea -->
      <div class="control-group">
        <label class="control-label" for="textarea">Description</label>

        <div class="controls">
          <%= f.input :description, label: false %>
        </div>
      </div>

      <!-- Button -->
      <div class="control-group">
        <label class="control-label" for="singlebutton"></label>

        <div class="controls">
          <div class="btn-group">
            <%= f.submit "Submit", :class => "btn btn-primary" %>
            <%= link_to 'All School medium ', admin_school_media_path, :class => "btn" %>
            <%= link_to 'cancel', new_admin_school_medium_path, :class => "btn btn-danger" %>
          </div>
        </div>
      </div>

    </fieldset>
<% end %>