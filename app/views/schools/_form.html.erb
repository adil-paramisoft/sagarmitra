<%= simple_form_for(@school) do |f| %>
  <% if @school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school.errors.count, "error") %> prohibited this school from being saved:</h2>

      <ul>
      <% @school.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :name %><br>
  </div>

  <div class="field">
    <%= f.input :address %><br>
  </div>
  <div class="field">
    <%= f.association :school_type %><br>
  </div>
  <div class="field">
    <%= f.association :program_state %><br>
  </div>
  <div class="field">
    <%= f.input :total_students %><br>
  </div>
  <div class="field">
    <%= f.association :school_medium %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
