
<h3>Schools in Sagar-Mitra Program</h3>
<div style="float:right; padding:10px">
	<%= link_to 'Start program in your school!', new_school_volunteer_path , :class => 'btn btn-medium btn-primary'%>
</div>

<table cellpadding="0" cellspacing="0"  class="table table-striped table-bordered" id="school_table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>School type</th>
      <th>Program state</th>
      <th>Total students</th>
      <th>Medium</th>
      <th>Plastic Collected(Kgs)</th>
      <th>Green Fund(INR )</th>
   <% if can? :create, School %>  
     <th>Actions</th> 
  <%end%>

    </tr>
  </thead>

  <tbody>
    <% @schools.each do |school| %>
      <tr>
	    <td><%=  school.name  %></td>
        <td><%= school.address %></td>
        <td><%= school.school_type.name %></td>
        <td><%= school.program_state.name %></td>
        <td><%= school.total_students %></td>
        <td><%= school.school_medium.name %></td>
        <%  plastic_collection_details = school.plastic_collection_details %>
        <td><%= plastic_collection_details[:plastic_collected] %> </td>
        <td><%= plastic_collection_details[:green_fund] %> </td>
<% if can? :create, School %>
        <td><%= link_to 'Show', school %> |
        <%= link_to 'Edit', edit_school_path(school) %> |
        <%= link_to 'Destroy', school, method: :delete, data:{ confirm: 'Are you sure?' } %></td>
<%end%>
      </tr>
    <% end %>
  
</table>

<br>
<% if can? :create, School %>
   <%= link_to 'New School', new_school_path %>
<%end%>

<script type="text/javascript">
    $(document).ready(function () {
        $('#school_table').dataTable( {
            "aaSorting": [[ 6, "desc" ]]
        } );
    });
</script>
